spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exchange_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379

exchange:
  api:
    auth-key: ${EXCHANGE_API_KEY:}
    base-url: https://oapi.koreaexim.go.kr/site/program/financial/exchangeJSON
